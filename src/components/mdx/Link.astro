---
const { href, ...rest } = Astro.props;
---

<a
  href={href}
  target="_blank"
  rel="noopener noreferrer"
  class="blog-link"
  {...rest}
>
  <slot />
</a>

<style>
  .blog-link,
  .blog-link:visited {
    color: mediumseagreen;
    text-decoration: none;
  }

  :global(html[data-theme="light"]) .blog-link,
  :global(html[data-theme="light"]) .blog-link:visited {
    color: tomato;
  }

  :global(html[data-theme="dark"]) .blog-link,
  :global(html[data-theme="dark"]) .blog-link:visited {
    color: greenyellow;
  }

  @media (prefers-color-scheme: light) {
    .blog-link,
    .blog-link:visited {
      color: tomato;
    }
  }

  @media (prefers-color-scheme: dark) {
    .blog-link,
    .blog-link:visited {
      color: greenyellow;
    }
  }

  .blog-link:hover {
    text-decoration: underline;
  }
  .blog-link:focus-visible {
    outline: 2px solid currentColor;
    outline-offset: 2px;
  }
</style>
